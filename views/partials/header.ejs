<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Проект</title>
    <link rel="icon" type="image/x-icon" href="/assets/images/favicon.ico" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <link ref="stylesheet" href="/assets/css/base.css" />
  </head>
  <body>
    <header class="p-2 bg-dark text-white mb-4">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            <li><a href="/" class="nav-link px-2 text-white">Главная</a></li>
          </ul>
          <div class="text-end">
            <% if(tokenData) { %>
            <form action="/api/auth/logout" method="post">
              <button type="submit" class="btn btn-secondary">Выход</button>
            </form>
            <% } else { %>
              <a href="/auth/login" class="btn btn-success me-2">Войти</a>
              <a href="/auth/signup" class="btn btn-outline-secondary">Регистрация</a>
            <% } %>
          </div>
        </div>
      </div>
    </header>
    <main class="container">
